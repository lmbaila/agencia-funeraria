# Copie para ".env" na raiz do projecto e preencha com valores reais antes de "docker compose up".
# O docker-compose.yml lê estas variáveis automaticamente a partir deste ficheiro.
JWT_SECRET=
CORS_ORIGIN=http://localhost:8080
ADMIN_IDENTIFIER=admin
ADMIN_PASSWORD=
